import styled from '@emotion/styled'

export const ParticipantTileRoot = styled.div`
  display: flex;
  align-items: center;
  width: 100%;
  padding: 10px 40px;
  border-bottom: ${({ theme }) => theme.commons.borders.main};
`
