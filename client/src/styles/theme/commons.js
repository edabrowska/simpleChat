import { border } from './colors'

export const borders = {
  main: `1px solid ${border.chat}`,
  input: `2px solid ${border.input}`,
  transparent: `1px solid ${border.transparent}`,
}

export const borderRadius = {
  main: '9px'
}